import { Link } from '@/components/Link'
import { BatiWidget } from './BatiWidget.tsx'
import BatiNote from './BatiNote.tsx'
import { UiFrameworkExtension } from '@/components/UiFrameworkExtension'

export const docConfig = {
  tableOfContents: false,
}

使用 Bati 来编排你的应用脚手架流程。Bati 是一个面向 JavaScript/TypeScript 应用的开源脚手架工具，提供 CLI 和 Web UI，可用于搭建集成了 Vike 及其扩展的应用。

<BatiWidget />
<BatiNote />

> **要求**: [Node.js](https://nodejs.org)（或 Deno/Bun），其中包含包管理器 `npm`（也可以使用 `pnpm`/`yarn`）。

> **脚手架生成的应用会使用 Vike 扩展**，例如 <UiFrameworkExtension name />。你也可以<Link href="/new/core">脚手架仅使用 Vike 核心的应用</Link>（不使用扩展）。 */}

## 另见

- <Link href="/new/core" />
- <Link href="/add" />
